from CG_lab1 import test, MyImage


if __name__ == "__main__":
    # №1
    # test.test()

    # №2, №3
    MyImage.test_lines()

    # №4, №5, №6, №7
    # file_path = './obj_files/deer.obj'
    # fox_lambda = lambda x: int(x * 7 + 300)
    # rabbit_lambda = lambda x: int(x * 4000 + 400)
    # deer_lambda = lambda x: int(x * 0.40 + 400)
    # MyImage.test_obj_model(file_path, prepare=deer_lambda)